# Audit — Boucle de jeu solo & anti-triche (L4) — 2026-06-19

- **Auteur** : agent REVUE-ANTITRICHE (Opus), boucle multi-agents.
- **Périmètre** : `apps/api/src/app.ts` (/play/*), `apps/api/src/play/*` + tests.
- **Référence** : `security-threat-model.md` (T-C1..C8), `feature-play-loop.md` (PLAY-D-1..9).

## Findings & état

| ID | Sév. | Description | État |
|----|------|-------------|------|
| F1 | 🟠 Élevé | Rate-limit global IP-seul sur `/play/*` (brute-force boards, pas de clé compte). | ✅ **Corrigé** : rate-limit **par route** (start 60/min, submit 30/min) + `keyGenerator` IP+session. |
| F4 | 🟡 Moyen | Pas de `bodyLimit` → board géant. | ✅ **Corrigé** : `bodyLimit` 16 Ko sur `/play/submit` (un board 9×9 < 1 Ko). |
| F3 | 🟡 Moyen | Secret HMAC token = `COOKIE_SECRET` (réutilisation). | ✅ **Corrigé** : `PLAY_TOKEN_SECRET` dédié (défaut COOKIE_SECRET). |
| F6 | 🟢 Faible | `attemptId` exposé inutilement dans la réponse start. | ✅ **Corrigé** : retiré (vit dans le token signé). |
| F2/F5 | 🟡/🟢 | `timeMs` = réception→soumission (pas →fin validation) ; 500 `puzzle_gone` cosmétique. | ⏳ Noté (in-memory : inatteignable ; revoir à la bascule Postgres). |
| F7 | ℹ️ | Plancher anti-bot constant 1 s contournable en attendant. | ⏳ Par conception (T-C4/C5 = tournoi). |

## Couverture T-C* (solo)
T-C1 (serveur autoritatif) ✅ · T-C2 (temps serveur, client ignoré) ✅ · T-C3 (attempt lié, hors-session rejeté) ✅ · T-C6 (usage unique, rejeu 409) ✅ · T-C7 (aucun endpoint d'écriture score/XP) ✅ · FND-D-20 (aucune fuite de solution, engine/server jamais côté front) ✅. **Tests** : 10 d'intégration + 4 token.

## À faire pour les tournois (hors L4, par conception)
T-C4 (1 attempt/user/puzzle non réinitialisable, fenêtre courte, détection de clusters), T-C5 (moves incrémentaux rejoués, plancher par jeu/difficulté), T-C8 (gain XP serveur depuis seed/difficulty — L5).

## Verdict
Anti-triche **solo solide** (0 critique). Quick-wins F1/F3/F4/F6 corrigés. Reste : tournoi (T-C4/C5/C8) et bascule Prisma (rétention des puzzles référencés).

## Front (même jalon)
Plateau **Tango jouable** hors-ligne (samples) avec **validation live** via `@puzzlehub/engine` client (sans fuite de solution), tutoriel intégré, sélecteur de difficulté, lazy-load via contrat de registre (RF-14). **Reste** : plateau Queens, wiring front↔API (start/submit), tutoriels interactifs guidés.
